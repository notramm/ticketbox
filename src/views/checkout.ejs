<section class="checkout">
  <h1 class="checkout__title">Checkout</h1>
  <p class="checkout__lede">
    Payment checkout lands on Day 4 (Razorpay). Your details were received for
    <strong><%= slug || 'this event' %></strong>.
  </p>
  <p><a href="<%= slug ? '/events/' + slug : '/' %>">Back to event</a></p>
</section>
